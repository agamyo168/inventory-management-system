## TODOS:

- [x] Fix eslint and prettier errors.
- [x] Fix book copies calculation bug
- [x] Fix Authors bug due to using findAll + transaction
- [x] test row by row insertion vs batching => batching is faster but it's negligible for such task.
- [ ] Add JOI Validation per ROW! in inventory service OR just simple parseInt for pages and copies
- [ ] Move to Sequelize v7
- [ ] Add sequelize database migrations
- [ ] Re-write in OOP and Clean Architecture + Repository pattern. (Optional?)

## Learned:

- [ ] Need a starter node/express boilerplate -> ESlint, Prettier, Jest, CSV, PDF, Multer, and Sequelize READY!! -> Maybe make a `bash script` or a `nix flake`?
- [ ] Use an OOP boilerplate to be as close to Nest logic next time with sequelize-typescript and decorators if possible!
- [ ] Should I start using Zod for both my Nest.js and Node projects?
